# [Cross-community vocabulary indexes](@id vocabulary-indexes)

**Page status:** generated bidirectional vocabulary index; not a standards crosswalk.

This page is generated from `vocabulary/vocabulary.toml`. It does not declare
community terms to be synonyms. Each row records a dominant relation class, an
editorial usage status, a diagnostic question, and a route to the maintained
definition. The conceptual introduction remains [One network, five languages](@ref
one-network-five-languages); the compact house definitions remain on the
[Terminology](@ref reference-terminology) page.
This page answers “how does a community phrase map to the house vocabulary?”; for claim,
chapter, and verification retrieval, use the [knowledge-base indexes](knowledge-base-index.md).

The five target communities are indexed separately. Circuit theory is included
after them as shared technical vocabulary rather than as a sixth audience route.

## Relation and usage keys

The **relation class** describes the map between a community phrase and the house
vocabulary. The **usage status** describes how the phrase may be used in this book.
These axes are orthogonal.

| Registry value | Reader-facing meaning |
| --- | --- |
| `exact_alias` | exact alias |
| `scoped_alias` | scoped alias |
| `broader_narrower` | broader / narrower |
| `representation_dependent` | representation dependent |
| `false_friend` | false friend |
| `preferred_house_term` | preferred house term |
| `accepted_qualified_shorthand` | accepted qualified shorthand |
| `unsafe_unqualified_term` | unsafe unqualified term |

## Community-to-book index

### Power engineering

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `bus`; `node`; `terminal` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `line`; `branch`; `circuit` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `device model`; `power factor` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `network graph`; `topology`; `one-line` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `from bus`; `to bus`; `upstream`; `downstream` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `power flow`; `line flow`; `branch current` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `parallel lines`; `double circuit`; `same corridor` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `radial feeder`; `mesh`; `loop` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `system state`; `operating state`; `switch state` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `rating`; `thermal limit`; `emergency rating` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `equivalent circuit`; `network equivalent` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `network reduction`; `equivalencing`; `aggregation` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `per-unit normalization`; `base conversion` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `line loss`; `network loss` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `ground`; `earth`; `neutral`; `solidly grounded` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `phase`; `phase conductor`; `positive sequence` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `Y-bus`; `bus admittance matrix` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

### Software and network data

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `equipment`; `terminal`; `connectivity node`; `topological node` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `conducting equipment`; `branch record`; `terminal pair` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `component model`; `equipment model` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `node-breaker topology`; `bus-branch topology`; `connectivity graph` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `terminal 1`; `terminal 2`; `from`; `to` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `from-end flow`; `to-end flow`; `measurement` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `duplicate endpoint pair`; `parallel equipment records` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `radial topology`; `island`; `loop` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `status`; `profile`; `state variable` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `normal amps`; `limit set`; `operational limit` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `equivalent model`; `round trip` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `conversion`; `topology processing`; `compilation` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `schema normalization`; `canonical ordering` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `data loss`; `loss of provenance` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `node 0`; `ground bus`; `neutral terminal`; `reference bus` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `phase code`; `terminal number`; `conductor order` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `Ybus`; `system Y matrix` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

### Mathematical modelling and optimization

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `node`; `variable block`; `index` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `branch`; `arc`; `relation` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `constraint relation`; `factor` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `equation graph`; `dependency graph`; `sparsity pattern` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `positive direction`; `oriented arc`; `causal dependency` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `flow variable`; `injection`; `transfer` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `parallel branches`; `aggregate branch` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `cycle basis`; `tree formulation`; `radiality constraint` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `state variable`; `scenario`; `decision state` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `constraint`; `bound`; `active constraint` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `exact reformulation`; `relaxation`; `equivalent feasible set` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `elimination`; `projection`; `reformulation` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `variable scaling`; `coordinate normalization` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `objective`; `penalty`; `loss` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `reference node`; `ground constraint`; `eliminated neutral` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `phase index`; `coordinate`; `sequence component` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `nodal admittance operator`; `assembled linear operator` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

### Mathematical graph theory

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `vertex`; `node` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `edge`; `arc` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `factor node`; `hyperedge` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `graph`; `adjacency`; `incidence structure` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `orientation`; `directed edge`; `rooted tree` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `network flow`; `circulation` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `parallel edges`; `edge multiplicity` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `cycle`; `forest`; `bridge`; `chord` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `graph state`; `dynamic graph` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `capacity`; `edge weight` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `isomorphism`; `quotient`; `minor` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `quotient`; `contraction`; `minor`; `coarsening` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `canonical labelling`; `normal form` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `information loss`; `forgotten labels` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `distinguished vertex`; `contracted ground node` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `layer`; `vertex colour`; `fibre coordinate` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `weighted support operator` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

### Graph machine learning

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `node`; `node type` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `edge`; `edge type`; `relation` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `factor node`; `relation type` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `input graph`; `message graph`; `adjacency` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `message direction`; `directed relation` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `message`; `aggregation` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `multi-edge`; `duplicate relation` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `graph cycle`; `tree architecture` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `hidden state`; `node state`; `graph state` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `constraint feature`; `feasibility label` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `expressive equivalence`; `invariant representation` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `pooling`; `coarsening`; `readout` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `feature normalization`; `graph normalization` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `training loss`; `objective` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `ground node`; `reference feature`; `neutral node` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `phase feature`; `channel`; `positional encoding` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `physics operator`; `graph shift operator` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

### Circuit theory (shared technical vocabulary)

| Familiar terms | House terms | Relation / usage | Diagnostic question | Unsafe inference | Route |
| --- | --- | --- | --- | --- | --- |
| `node`; `terminal`; `port` | `asset`; `junction`; `port`; `terminal`; `vertex` | representation dependent; unsafe unqualified term | What object type and representation does the word name? | Every node is one physical bus. | [definition](terminology.md#terminology-bus) |
| `branch`; `one-port`; `two-port` | `asset`; `line`; `oriented attachment`; `support edge` | representation dependent; unsafe unqualified term | What owns identity, and what records attachment or algebraic coupling? | Every edge is one physical branch. | [definition](terminology.md#terminology-line) |
| `constitutive relation`; `multiport` | `factor`; `behavioural relation` | false friend; unsafe unqualified term | Does factor mean a behavioural relation, factor-graph node, power factor, or matrix factorization? | The shared word denotes the same mathematical object. | [definition](../foundations/formal-representation-frameworks.md) |
| `circuit graph`; `network topology`; `incidence` | `named graph`; `typed incidence`; `active topology`; `support graph` | representation dependent; unsafe unqualified term | Which asset, connectivity, factor, equation, support, or message graph is meant? | One topology answers every physical and computational query. | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `reference direction`; `passive sign convention` | `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | scoped alias; accepted qualified shorthand | Is direction a stored order, sign convention, operating result, rooted hierarchy, causal relation, or admissibility rule? | An arrow predicts physical transfer or permanent upstream/downstream status. | [definition](terminology.md#terminology-directed) |
| `branch current`; `terminal current`; `complex power` | `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | false friend; unsafe unqualified term | Which terminal or internal quantity is meant, and which conservation law applies? | One antisymmetric conserved scalar lives on every edge. | [definition](terminology.md#terminology-flow) |
| `parallel connection`; `common terminal pair` | `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | broader / narrower; accepted qualified shorthand | Do members share endpoints, terminals, a corridor, constitutive data, or operating interchangeability? | Parallel members can be merged without a preservation contract. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `loop`; `mesh`; `loop current` | `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | representation dependent; unsafe unqualified term | In which graph and active state is the cycle, tree, mesh, or radiality claim made? | A graph cycle is a circulating physical flow, or radiality is representation independent. | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `circuit state`; `initial condition` | `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | false friend; unsafe unqualified term | Is state an electrical variable, equipment status, scenario, estimator object, or learned hidden representation? | One state container gives all meanings the same semantics. | [definition](../start/one-network-five-languages.md) |
| `admissible region`; `device limit` | `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | broader / narrower; unsafe unqualified term | Is this a source datum, operational policy, mathematical encoding, or solution status? | One scalar constraint reproduces the complete equipment limit. | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `port equivalence`; `driving-point equivalence` | `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | scoped alias; unsafe unqualified term | Which interface, domain, observation, constraint, decision, and recovery object is preserved? | Equal terminal equations imply equal feasible decisions and source meaning. | [definition](terminology.md#terminology-equivalent) |
| `network reduction`; `equivalent circuit`; `elimination` | `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | broader / narrower; unsafe unqualified term | Which objects are renamed, generated, forgotten, solved, aggregated, or recoverable? | Every transformation merely makes the graph smaller. | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `reference scaling`; `per-unit conversion` | `coordinate normalization` | false friend; unsafe unqualified term | Is normalization changing units, coordinates, schemas, physical parameters, or statistical features? | All normalizations preserve the same semantics and admit the same inverse. | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `dissipation`; `absorbed power` | `electrical loss`; `information loss`; `objective function` | false friend; unsafe unqualified term | Is loss absorbed power, unrecoverable information, or an optimization/training objective? | Reducing one kind of loss reduces the others. | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `datum node`; `ground`; `reference`; `return conductor` | `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | false friend; unsafe unqualified term | Is this physical earth, a conductor, an impedance or connection, or a gauge reference? | All are one zero-voltage node. | [definition](terminology.md#terminology-ground-neutral-reference) |
| `phase-domain coordinate`; `sequence component`; `terminal variable` | `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | representation dependent; unsafe unqualified term | Does phase name an asset label, physical path, terminal slot, phase-domain component, or transformed coordinate? | A matching phase label fixes every terminal and coordinate map. | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `nodal admittance matrix` | `declared nodal operator` | exact alias; preferred house term | Are the coordinate order and model class fixed, and is the alias restricted to the assembled operator? | An operator alias is also an alias for the source network or its assembly decomposition. | [definition](../foundations/notation-and-conventions.md) |

## Book-to-community index

Use this view when a house term is familiar but the language likely to appear in
another community is not. Blank cells are meaningful: the registry does not invent
an alias merely to complete a row.

| House-term cluster | Power engineering | Software / data | Mathematical modelling | Graph theory | Graph ML | Circuit theory | Relation | Route |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `asset`; `junction`; `port`; `terminal`; `vertex` | `bus`; `node`; `terminal` | `equipment`; `terminal`; `connectivity node`; `topological node` | `node`; `variable block`; `index` | `vertex`; `node` | `node`; `node type` | `node`; `terminal`; `port` | representation dependent | [definition](terminology.md#terminology-bus) |
| `asset`; `line`; `oriented attachment`; `support edge` | `line`; `branch`; `circuit` | `conducting equipment`; `branch record`; `terminal pair` | `branch`; `arc`; `relation` | `edge`; `arc` | `edge`; `edge type`; `relation` | `branch`; `one-port`; `two-port` | representation dependent | [definition](terminology.md#terminology-line) |
| `factor`; `behavioural relation` | `device model`; `power factor` | `component model`; `equipment model` | `constraint relation`; `factor` | `factor node`; `hyperedge` | `factor node`; `relation type` | `constitutive relation`; `multiport` | false friend | [definition](../foundations/formal-representation-frameworks.md) |
| `named graph`; `typed incidence`; `active topology`; `support graph` | `network graph`; `topology`; `one-line` | `node-breaker topology`; `bus-branch topology`; `connectivity graph` | `equation graph`; `dependency graph`; `sparsity pattern` | `graph`; `adjacency`; `incidence structure` | `input graph`; `message graph`; `adjacency` | `circuit graph`; `network topology`; `incidence` | representation dependent | [definition](../foundations/representation-taxonomy.md#representation-taxonomy) |
| `stored orientation`; `terminal sign`; `operating transfer`; `rooted-tree relation` | `from bus`; `to bus`; `upstream`; `downstream` | `terminal 1`; `terminal 2`; `from`; `to` | `positive direction`; `oriented arc`; `causal dependency` | `orientation`; `directed edge`; `rooted tree` | `message direction`; `directed relation` | `reference direction`; `passive sign convention` | scoped alias | [definition](terminology.md#terminology-directed) |
| `series current`; `terminal current`; `terminal power`; `operating transfer`; `message` | `power flow`; `line flow`; `branch current` | `from-end flow`; `to-end flow`; `measurement` | `flow variable`; `injection`; `transfer` | `network flow`; `circulation` | `message`; `aggregation` | `branch current`; `terminal current`; `complex power` | false friend | [definition](terminology.md#terminology-flow) |
| `corridor parallelism`; `parallel fibre`; `terminal parallelism`; `electrical aggregability`; `coupling group` | `parallel lines`; `double circuit`; `same corridor` | `duplicate endpoint pair`; `parallel equipment records` | `parallel branches`; `aggregate branch` | `parallel edges`; `edge multiplicity` | `multi-edge`; `duplicate relation` | `parallel connection`; `common terminal pair` | broader / narrower | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `simple cycle`; `line-identity cycle`; `adjacency-radial`; `member-radial`; `rooted active-tree view` | `radial feeder`; `mesh`; `loop` | `radial topology`; `island`; `loop` | `cycle basis`; `tree formulation`; `radiality constraint` | `cycle`; `forest`; `bridge`; `chord` | `graph cycle`; `tree architecture` | `loop`; `mesh`; `loop current` | representation dependent | [definition](../foundations/cycles-parallelism-radiality.md#cycles-parallelism-radiality) |
| `electrical state`; `equipment state`; `scenario`; `estimator state`; `hidden state` | `system state`; `operating state`; `switch state` | `status`; `profile`; `state variable` | `state variable`; `scenario`; `decision state` | `graph state`; `dynamic graph` | `hidden state`; `node state`; `graph state` | `circuit state`; `initial condition` | false friend | [definition](../start/one-network-five-languages.md) |
| `rating`; `limit record`; `feasible region`; `constraint`; `binding status` | `rating`; `thermal limit`; `emergency rating` | `normal amps`; `limit set`; `operational limit` | `constraint`; `bound`; `active constraint` | `capacity`; `edge weight` | `constraint feature`; `feasibility label` | `admissible region`; `device limit` | broader / narrower | [definition](../foundations/rating-semantics.md#rating-semantics) |
| `preservation contract`; `exact normalization`; `behavioural equivalence`; `decision equivalence` | `equivalent circuit`; `network equivalent` | `equivalent model`; `round trip` | `exact reformulation`; `relaxation`; `equivalent feasible set` | `isomorphism`; `quotient`; `minor` | `expressive equivalence`; `invariant representation` | `port equivalence`; `driving-point equivalence` | scoped alias | [definition](terminology.md#terminology-equivalent) |
| `projection`; `compilation`; `normalization`; `elimination`; `aggregation`; `reduction` | `network reduction`; `equivalencing`; `aggregation` | `conversion`; `topology processing`; `compilation` | `elimination`; `projection`; `reformulation` | `quotient`; `contraction`; `minor`; `coarsening` | `pooling`; `coarsening`; `readout` | `network reduction`; `equivalent circuit`; `elimination` | broader / narrower | [definition](../foundations/transformation-semantics-register.md#transformation-semantics-register) |
| `coordinate normalization` | `per-unit normalization`; `base conversion` | `schema normalization`; `canonical ordering` | `variable scaling`; `coordinate normalization` | `canonical labelling`; `normal form` | `feature normalization`; `graph normalization` | `reference scaling`; `per-unit conversion` | false friend | [definition](../transformations/conductor-coordinate-normalization.md#conductor-coordinate-normalization) |
| `electrical loss`; `information loss`; `objective function` | `line loss`; `network loss` | `data loss`; `loss of provenance` | `objective`; `penalty`; `loss` | `information loss`; `forgotten labels` | `training loss`; `objective` | `dissipation`; `absorbed power` | false friend | [definition](../foundations/numerical-consequences.md#numerical-consequences) |
| `physical earth`; `neutral conductor`; `grounding factor`; `voltage reference` | `ground`; `earth`; `neutral`; `solidly grounded` | `node 0`; `ground bus`; `neutral terminal`; `reference bus` | `reference node`; `ground constraint`; `eliminated neutral` | `distinguished vertex`; `contracted ground node` | `ground node`; `reference feature`; `neutral node` | `datum node`; `ground`; `reference`; `return conductor` | false friend | [definition](terminology.md#terminology-ground-neutral-reference) |
| `asset phase label`; `conductor`; `terminal coordinate`; `phase-domain component`; `sequence coordinate` | `phase`; `phase conductor`; `positive sequence` | `phase code`; `terminal number`; `conductor order` | `phase index`; `coordinate`; `sequence component` | `layer`; `vertex colour`; `fibre coordinate` | `phase feature`; `channel`; `positional encoding` | `phase-domain coordinate`; `sequence component`; `terminal variable` | representation dependent | [definition](terminology.md#terminology-phase-conductor-coordinate) |
| `declared nodal operator` | `Y-bus`; `bus admittance matrix` | `Ybus`; `system Y matrix` | `nodal admittance operator`; `assembled linear operator` | `weighted support operator` | `physics operator`; `graph shift operator` | `nodal admittance matrix` | exact alias | [definition](../foundations/notation-and-conventions.md) |

## Scope and review boundary

This first registry contains 17 collision concepts. It is a controlled
book vocabulary, not an empirical claim that every practitioner uses the recorded
terms identically. Community review remains required. Changes should update the
registry first and regenerate this page rather than editing the generated tables.
